{
  "id": "hero-animated",
  "name": "Animated Hero",
  "category": "landing",
  "description": "Modern hero with animated gradients and floating elements",
  "version": "1.0.0",
  "props": {
    "headline": {
      "type": "string",
      "required": true,
      "aiGenerate": true
    },
    "subtext": {
      "type": "string",
      "required": true,
      "aiGenerate": true
    },
    "primaryCta": {
      "type": "object",
      "required": true,
      "properties": {
        "text": { "type": "string", "default": "Get Started Free" },
        "href": { "type": "string", "default": "/signup" }
      }
    },
    "secondaryCta": {
      "type": "object",
      "required": false
    },
    "gradientColors": {
      "type": "object",
      "required": false,
      "properties": {
        "from": { "type": "string", "default": "from-violet-600" },
        "via": { "type": "string", "default": "via-purple-600" },
        "to": { "type": "string", "default": "to-indigo-600" }
      }
    }
  },
  "bestFor": ["tech", "startup", "modern"],
  "tags": ["animation", "gradient", "modern"],
  "dependencies": []
}

