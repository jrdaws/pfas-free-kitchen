/**
 * CTA Section - Simple
 * 
 * Clean call-to-action with headline and button.
 * Generated by {{projectName}}
 */

import Link from "next/link";

interface CtaSimpleProps {
  headline?: string;
  subtext?: string;
  ctaText?: string;
  ctaLink?: string;
  bgColor?: "primary" | "dark" | "gradient";
}

export function CtaSimple({
  headline = "{{headline}}",
  subtext = "{{subtext}}",
  ctaText = "{{ctaText}}",
  ctaLink = "{{ctaLink}}",
  bgColor = "{{bgColor}}",
}: CtaSimpleProps) {
  const bgClasses = {
    primary: "bg-primary",
    dark: "bg-gray-900",
    gradient: "bg-gradient-to-r from-primary to-purple-600",
  };

  return (
    <section className={`py-20 ${bgClasses[bgColor] || bgClasses.primary}`}>
      <div className="container mx-auto px-4">
        <div className="max-w-3xl mx-auto text-center">
          <h2 className="text-3xl md:text-4xl font-bold text-white mb-4">
            {headline}
          </h2>
          
          {subtext && (
            <p className="text-xl text-white/80 mb-8">
              {subtext}
            </p>
          )}

          <Link
            href={ctaLink}
            className="inline-flex px-8 py-4 bg-white text-gray-900 font-semibold rounded-lg hover:bg-gray-100 transition-colors"
          >
            {ctaText}
          </Link>
        </div>
      </div>
    </section>
  );
}

export default CtaSimple;

