# P0 Task: Implement Navy Theme Across ENTIRE Website

> **Priority**: P0 - Critical
> **From**: Quality Agent
> **Created**: 2025-12-25
> **Type**: Complete Theme Implementation

---

## Objective

Apply the **Navy/Dark theme** consistently across the **ENTIRE website**, including:
- ✅ Landing page (homepage)
- ✅ **Project Configurator** (all steps/phases)
- ✅ All marketing pages
- ✅ Documentation pages (if any)
- ✅ Platform pages
- ✅ All components and UI elements

The Navy theme must be the DEFAULT and applied EVERYWHERE.

---

## Changes Required

### 1. Update CSS Variables - Dark as Default

```css
/* globals.css */
@layer base {
  /* DARK THEME (Navy) - NOW DEFAULT */
  :root {
    --background: 222 47% 11%;       /* #0F172A - Slate-900 */
    --background-alt: 217 33% 17%;   /* #1E293B - Slate-800 */
    --foreground: 210 40% 98%;       /* #F8FAFC */
    --foreground-secondary: 214 20% 80%;
    --foreground-muted: 215 16% 65%;
    
    --card: 217 33% 17%;
    --card-foreground: 210 40% 98%;
    
    --primary: 25 95% 53%;           /* #F97316 - Orange */
    --primary-foreground: 0 0% 100%;
    --primary-hover: 27 96% 61%;
    
    --accent: 25 95% 53%;
    --accent-light: rgba(249, 115, 22, 0.15);
    
    --border: 217 19% 27%;
    --ring: 25 95% 53%;
    
    --terminal-bg: 222 47% 11%;
    --terminal-header: 217 33% 17%;
    --terminal-text: 215 16% 65%;
    
    --hero-bg: 210 52% 25%;          /* Navy #1E3A5F */
    --footer-bg: 210 52% 25%;        /* Navy #1E3A5F */
    
    --success: 160 72% 51%;
    --warning: 43 96% 56%;
    --destructive: 0 72% 63%;
  }

  /* LIGHT THEME (Warm Sand) - Optional */
  .light {
    --background: 40 33% 98%;
    --background-alt: 35 43% 97%;
    --foreground: 24 10% 16%;
    --foreground-secondary: 25 8% 33%;
    --foreground-muted: 25 6% 45%;
    
    --card: 0 0% 100%;
    --card-foreground: 24 10% 16%;
    
    --primary: 21 90% 48%;
    --primary-foreground: 0 0% 100%;
    --primary-hover: 21 91% 42%;
    
    --accent: 24 94% 53%;
    --accent-light: 33 100% 96%;
    
    --border: 24 6% 90%;
    --ring: 21 90% 48%;
    
    --terminal-bg: 24 10% 16%;
    --terminal-header: 25 8% 26%;
    --terminal-text: 24 5% 64%;
    
    --hero-bg: 35 43% 97%;
    --footer-bg: 24 10% 16%;
    
    --success: 160 84% 39%;
    --warning: 38 92% 50%;
    --destructive: 0 84% 60%;
  }
}
```

### 2. Update ThemeToggle Component

```tsx
// components/ui/ThemeToggle.tsx
'use client';

import { useEffect, useState } from 'react';
import { Moon, Sun } from 'lucide-react';

export function ThemeToggle() {
  const [theme, setTheme] = useState<'light' | 'dark'>('dark'); // DEFAULT: dark

  useEffect(() => {
    const saved = localStorage.getItem('theme');
    
    // Only switch to light if explicitly saved
    if (saved === 'light') {
      setTheme('light');
      document.documentElement.classList.add('light');
    }
    // Dark is now default, no class needed
  }, []);

  const toggleTheme = () => {
    const newTheme = theme === 'dark' ? 'light' : 'dark';
    setTheme(newTheme);
    
    if (newTheme === 'light') {
      document.documentElement.classList.add('light');
    } else {
      document.documentElement.classList.remove('light');
    }
    
    localStorage.setItem('theme', newTheme);
  };

  return (
    <button
      onClick={toggleTheme}
      className="fixed bottom-6 right-6 z-50 p-3 rounded-full 
                 bg-card border border-border shadow-lg
                 hover:border-primary transition-all duration-200"
      aria-label="Toggle theme"
    >
      {theme === 'dark' ? (
        <Sun className="w-5 h-5 text-foreground-secondary" />
      ) : (
        <Moon className="w-5 h-5 text-foreground-secondary" />
      )}
    </button>
  );
}
```

### 3. Update Tailwind Config

```js
// tailwind.config.js
module.exports = {
  darkMode: 'class', // Keep this, but we use .light class now
  // ... rest of config
}
```

### 4. Body Base Styles

```css
@layer base {
  body {
    @apply bg-background text-foreground;
    /* Dark theme colors applied by default via :root */
  }
}
```

---

## Summary of Theme Logic

| Scenario | Result |
|----------|--------|
| First visit | Dark (Navy) theme - DEFAULT |
| User clicks sun icon | Light (Warm Sand) theme |
| User reloads after choosing light | Light theme persists |
| User clicks moon icon | Back to Dark theme |

---

---

## Pages to Update (ALL)

### 1. Landing Page (`app/page.tsx`)
- [x] Hero section: Navy background
- [x] Features section: Slate-900 background
- [x] All cards: Slate-800 with proper shadows
- [x] Terminal mockups: Consistent dark styling
- [x] Footer: Navy background

### 2. Project Configurator (`app/configurator/` or similar)

**This is CRITICAL - the configurator must match the landing page theme!**

| Configurator Element | Theme Styling |
|---------------------|---------------|
| **Sidebar** | `bg-slate-800` or `bg-[#1E293B]` |
| **Sidebar active item** | Orange left border, `bg-orange-500/10` |
| **Main content area** | `bg-slate-900` or `bg-[#0F172A]` |
| **Step cards** | `bg-slate-800 border-slate-700` |
| **Form inputs** | `bg-slate-800 border-slate-600 text-white` |
| **Buttons (primary)** | `bg-orange-500 text-white` |
| **Buttons (secondary)** | `bg-slate-700 text-white` |
| **Progress bar** | Orange gradient on slate background |
| **Phase indicators** | Orange for active, slate for inactive |
| **Preview panel** | `bg-slate-900` with subtle border |
| **File tree** | Slate backgrounds, orange highlights |

#### Configurator Sidebar Example:
```tsx
<aside className="w-80 bg-slate-800 border-r border-slate-700">
  {/* Logo */}
  <div className="p-4 border-b border-slate-700">
    <span className="text-white font-bold">Configurator</span>
  </div>
  
  {/* Nav items */}
  <nav className="p-2">
    {/* Active item */}
    <div className="flex items-center gap-3 p-3 rounded-lg bg-orange-500/10 border-l-4 border-orange-500">
      <div className="w-8 h-8 bg-orange-500/20 text-orange-500 rounded-lg flex items-center justify-center">
        Icon
      </div>
      <span className="text-white font-medium">Active Step</span>
    </div>
    
    {/* Inactive item */}
    <div className="flex items-center gap-3 p-3 rounded-lg hover:bg-slate-700/50">
      <div className="w-8 h-8 bg-slate-700 text-slate-400 rounded-lg flex items-center justify-center">
        Icon
      </div>
      <span className="text-slate-400">Inactive Step</span>
    </div>
    
    {/* Completed item */}
    <div className="flex items-center gap-3 p-3 rounded-lg">
      <div className="w-8 h-8 bg-emerald-500/20 text-emerald-500 rounded-lg flex items-center justify-center">
        ✓
      </div>
      <span className="text-slate-300">Completed Step</span>
    </div>
  </nav>
  
  {/* Progress */}
  <div className="p-4 border-t border-slate-700 mt-auto">
    <div className="h-2 bg-slate-700 rounded-full overflow-hidden">
      <div className="h-full bg-gradient-to-r from-orange-500 to-amber-500 w-[60%]" />
    </div>
    <span className="text-sm text-slate-400 mt-2">60% complete</span>
  </div>
</aside>
```

#### Configurator Main Content:
```tsx
<main className="flex-1 bg-slate-900 p-8">
  {/* Header */}
  <h1 className="text-2xl font-bold text-white mb-2">Step Title</h1>
  <p className="text-slate-400 mb-8">Step description...</p>
  
  {/* Form cards */}
  <div className="bg-slate-800 rounded-xl border border-slate-700 p-6">
    <label className="block text-sm font-medium text-slate-300 mb-2">
      Field Label
    </label>
    <input 
      className="w-full px-4 py-3 bg-slate-900 border border-slate-600 rounded-lg 
                 text-white placeholder-slate-500
                 focus:border-orange-500 focus:ring-1 focus:ring-orange-500"
      placeholder="Enter value..."
    />
  </div>
  
  {/* Action buttons */}
  <div className="flex gap-4 mt-8">
    <button className="px-6 py-3 bg-slate-700 text-white rounded-lg hover:bg-slate-600">
      Back
    </button>
    <button className="px-6 py-3 bg-orange-500 text-white rounded-lg hover:bg-orange-600 
                       shadow-lg shadow-orange-500/25">
      Continue
    </button>
  </div>
</main>
```

### 3. Platform Page (`app/platform/page.tsx`)
- Background: Slate-900
- All cards: Slate-800
- Accents: Orange

### 4. Any Other Pages
Apply the same Navy/Slate theme to ALL pages in `app/` directory.

---

## Component Files to Update

Search and update these component files:

| Component | Update Required |
|-----------|----------------|
| `components/configurator/*` | All to use Navy theme |
| `components/ui/Card.tsx` | Slate-800 bg, slate-700 border |
| `components/ui/Button.tsx` | Orange primary, slate secondary |
| `components/ui/Input.tsx` | Slate-900 bg, slate-600 border |
| `components/ui/Sidebar.tsx` | Slate-800 bg |
| `components/preview/*` | Dark theme styling |
| `components/marketing/*` | Navy/slate theme |

---

## Files to Search and Update

Run these searches to find hardcoded colors:

```bash
# Find any remaining light backgrounds
grep -r "bg-white" website/app/
grep -r "bg-stone-50" website/app/
grep -r "bg-gray-" website/app/

# Find light text that needs darkening
grep -r "text-gray-900" website/app/
grep -r "text-stone-900" website/app/

# Find borders that need updating  
grep -r "border-gray-" website/app/
grep -r "border-stone-200" website/app/
```

Replace with theme-aware classes:
- `bg-white` → `bg-card` or `bg-slate-800`
- `bg-stone-50` → `bg-background` or `bg-slate-900`
- `text-gray-900` → `text-foreground` or `text-white`
- `border-gray-200` → `border-border` or `border-slate-700`

---

## Validation Checklist

### A. Theme Default
- [ ] Fresh browser (no localStorage) → Navy theme loads
- [ ] Theme toggle works (light/dark switch)
- [ ] Theme persists across page refresh

### B. Landing Page
- [ ] Hero: Navy background with orange glow
- [ ] All sections: Slate-900 background
- [ ] Cards: Slate-800 with proper shadows
- [ ] Terminals: Consistent dark styling
- [ ] Footer: Navy background

### C. Project Configurator (CRITICAL)
- [ ] Sidebar: Slate-800 background
- [ ] Active step: Orange accent/border
- [ ] Completed steps: Emerald checkmarks
- [ ] Form inputs: Dark background, light text
- [ ] Buttons: Orange primary, slate secondary
- [ ] Preview panel: Dark theme
- [ ] File tree: Dark theme with orange highlights
- [ ] Progress bar: Orange on slate background
- [ ] NO white or light-colored sections

### D. All Other Pages
- [ ] Platform page: Navy/slate theme
- [ ] All marketing pages: Navy/slate theme
- [ ] No page should have a white/light background

### E. Overall Consistency
- [ ] Same dark theme across ALL pages
- [ ] No jarring color shifts when navigating
- [ ] Orange accents used consistently for CTAs
- [ ] All text is readable on dark backgrounds

---

## After Completion

```bash
npm run dev
```

**Test ALL pages:**
1. Homepage (http://localhost:3000)
2. **Configurator** (http://localhost:3000/configurator)
3. Platform (http://localhost:3000/platform)
4. Any other pages

**Take screenshots of:**
1. Homepage hero
2. **Configurator sidebar + main content**
3. Configurator preview panel
4. Footer

Share screenshots for final Quality Agent approval.

---

*Task created by Quality Agent | 2025-12-25*
*Approved theme: Navy/Dark with orange accents*

