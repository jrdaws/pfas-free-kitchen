# TASK: Template Gallery for Quick Start

**Priority**: P1
**Wave**: 4 (After Wave 3)
**Assigned To**: Platform Agent
**Estimated Effort**: 4 hours
**Dependencies**: Pattern library complete

---

## Objective

Create a template gallery that shows pre-designed website templates users can start from. Each template is a complete ProjectDefinition with branding, sections, and placeholder content.

---

## Current State

- Pattern library has all components
- DefinitionBuilder can create definitions
- No pre-built templates available
- Users must build from scratch

---

## Implementation

### 1. Template Definitions

**File**: `website/lib/patterns/templates/index.ts`

```typescript
import type { ProjectDefinition } from '../types';

export interface Template {
  id: string;
  name: string;
  description: string;
  category: 'saas' | 'agency' | 'ecommerce' | 'portfolio' | 'startup';
  thumbnail: string;
  tags: string[];
  definition: ProjectDefinition;
}

export const templates: Template[] = [
  saasTemplate,
  agencyTemplate,
  ecommerceTemplate,
  portfolioTemplate,
  startupTemplate,
];

export function getTemplateById(id: string): Template | undefined {
  return templates.find((t) => t.id === id);
}

export function getTemplatesByCategory(category: Template['category']): Template[] {
  return templates.filter((t) => t.category === category);
}
```

### 2. SaaS Template

**File**: `website/lib/patterns/templates/saas.ts`

```typescript
import type { Template } from './index';

export const saasTemplate: Template = {
  id: 'saas-standard',
  name: 'SaaS Pro',
  description: 'Modern SaaS landing page with pricing, features, and testimonials',
  category: 'saas',
  thumbnail: '/templates/saas-preview.png',
  tags: ['software', 'subscription', 'b2b', 'dark'],
  definition: {
    projectName: 'YourSaaS',
    domain: 'yoursaas.com',
    branding: {
      colors: {
        primary: '#6366F1',
        secondary: '#8B5CF6',
        accent: '#F59E0B',
        background: '#0F172A',
        surface: '#1E293B',
        text: '#F8FAFC',
        textMuted: '#94A3B8',
      },
      fonts: {
        heading: 'Inter',
        body: 'Inter',
        scale: 'default',
      },
      spacing: {
        sectionY: 'comfortable',
        containerWidth: 'default',
      },
      borderRadius: 'medium',
    },
    pages: [
      {
        path: '/',
        meta: {
          title: 'YourSaaS - Tagline Here',
          description: 'Description of your SaaS product',
        },
        sections: [
          {
            id: 'hero-1',
            patternId: 'hero-centered-gradient',
            props: {
              headline: 'Build faster with AI-powered tools',
              subheadline: 'Streamline your workflow and ship products 10x faster with our intelligent platform.',
              primaryCTA: { label: 'Start Free Trial', href: '/signup' },
              secondaryCTA: { label: 'Watch Demo', href: '#demo' },
              gradientFrom: '#6366F1',
              gradientTo: '#8B5CF6',
            },
          },
          {
            id: 'logos-1',
            patternId: 'logos-simple',
            props: {
              headline: 'Trusted by industry leaders',
              grayscale: true,
            },
          },
          {
            id: 'features-1',
            patternId: 'features-icon-grid',
            props: {
              title: 'Everything you need to succeed',
              subtitle: 'Powerful features designed for modern teams',
              features: [
                {
                  title: 'Lightning Fast',
                  description: 'Optimized performance that scales with your business',
                  icon: '⚡',
                },
                {
                  title: 'AI Powered',
                  description: 'Smart automation that learns from your workflow',
                  icon: '🤖',
                },
                {
                  title: 'Team Collaboration',
                  description: 'Work together seamlessly with real-time sync',
                  icon: '👥',
                },
                {
                  title: 'Enterprise Security',
                  description: 'Bank-grade encryption and compliance built-in',
                  icon: '🔒',
                },
                {
                  title: 'Analytics Dashboard',
                  description: 'Track performance with detailed insights',
                  icon: '📊',
                },
                {
                  title: '24/7 Support',
                  description: 'Expert help whenever you need it',
                  icon: '🎧',
                },
              ],
            },
          },
          {
            id: 'testimonials-1',
            patternId: 'testimonials-grid',
            props: {
              headline: 'Loved by thousands of customers',
            },
          },
          {
            id: 'pricing-1',
            patternId: 'pricing-three-tier',
            props: {
              headline: 'Simple, transparent pricing',
              subtitle: 'No hidden fees. Cancel anytime.',
            },
          },
          {
            id: 'faq-1',
            patternId: 'faq-accordion',
            props: {
              headline: 'Frequently asked questions',
            },
          },
          {
            id: 'cta-1',
            patternId: 'cta-simple',
            props: {
              headline: 'Ready to get started?',
              subheadline: 'Join thousands of teams already using YourSaaS',
              buttonText: 'Start Free Trial',
              buttonUrl: '/signup',
              variant: 'gradient-bg',
            },
          },
        ],
      },
    ],
  },
};
```

### 3. Agency Template

**File**: `website/lib/patterns/templates/agency.ts`

```typescript
import type { Template } from './index';

export const agencyTemplate: Template = {
  id: 'agency-creative',
  name: 'Creative Agency',
  description: 'Bold agency portfolio with case studies and team showcase',
  category: 'agency',
  thumbnail: '/templates/agency-preview.png',
  tags: ['design', 'creative', 'portfolio', 'light'],
  definition: {
    projectName: 'Studio X',
    domain: 'studiox.com',
    branding: {
      colors: {
        primary: '#18181B',
        secondary: '#3B82F6',
        accent: '#F97316',
        background: '#FAFAFA',
        surface: '#FFFFFF',
        text: '#18181B',
        textMuted: '#71717A',
      },
      fonts: {
        heading: 'Playfair Display',
        body: 'Inter',
        scale: 'large',
      },
      spacing: {
        sectionY: 'spacious',
        containerWidth: 'wide',
      },
      borderRadius: 'small',
    },
    pages: [
      {
        path: '/',
        meta: {
          title: 'Studio X - Creative Agency',
          description: 'We create stunning digital experiences',
        },
        sections: [
          {
            id: 'hero-1',
            patternId: 'hero-split-image',
            props: {
              headline: 'We craft digital experiences that inspire',
              description: 'Award-winning design studio specializing in brand identity, web design, and digital products.',
              primaryCTA: { label: 'View Our Work', href: '/work' },
            },
          },
          {
            id: 'features-1',
            patternId: 'features-bento',
            props: {
              title: 'Our Services',
            },
          },
          {
            id: 'testimonials-1',
            patternId: 'testimonials-featured',
            props: {
              headline: "What our clients say",
            },
          },
          {
            id: 'cta-1',
            patternId: 'cta-simple',
            props: {
              headline: "Let's create something amazing",
              subheadline: 'Get in touch to discuss your next project',
              buttonText: 'Start a Project',
              buttonUrl: '/contact',
              variant: 'centered',
            },
          },
        ],
      },
    ],
  },
};
```

### 4. Template Gallery Component

**File**: `website/components/configurator/TemplateGallery.tsx`

```tsx
'use client';

import { useState } from 'react';
import { templates, Template } from '@/lib/patterns/templates';

interface TemplateGalleryProps {
  onSelect: (template: Template) => void;
}

const CATEGORIES = [
  { id: 'all', label: 'All' },
  { id: 'saas', label: 'SaaS' },
  { id: 'agency', label: 'Agency' },
  { id: 'ecommerce', label: 'E-commerce' },
  { id: 'portfolio', label: 'Portfolio' },
  { id: 'startup', label: 'Startup' },
];

export function TemplateGallery({ onSelect }: TemplateGalleryProps) {
  const [activeCategory, setActiveCategory] = useState('all');

  const filteredTemplates =
    activeCategory === 'all'
      ? templates
      : templates.filter((t) => t.category === activeCategory);

  return (
    <div className="max-w-6xl mx-auto py-12 px-6">
      {/* Header */}
      <div className="text-center mb-12">
        <h1 className="text-4xl font-bold text-white mb-4">
          Start with a Template
        </h1>
        <p className="text-xl text-white/60">
          Choose a professionally designed template and customize it to match your brand
        </p>
      </div>

      {/* Category filter */}
      <div className="flex justify-center gap-2 mb-8">
        {CATEGORIES.map((cat) => (
          <button
            key={cat.id}
            onClick={() => setActiveCategory(cat.id)}
            className={`px-4 py-2 rounded-lg text-sm transition-colors ${
              activeCategory === cat.id
                ? 'bg-orange-500 text-white'
                : 'bg-slate-800 text-white/60 hover:text-white hover:bg-slate-700'
            }`}
          >
            {cat.label}
          </button>
        ))}
      </div>

      {/* Template grid */}
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {/* Blank template */}
        <BlankTemplateCard onSelect={() => onSelect(null)} />

        {filteredTemplates.map((template) => (
          <TemplateCard
            key={template.id}
            template={template}
            onSelect={() => onSelect(template)}
          />
        ))}
      </div>
    </div>
  );
}

function TemplateCard({
  template,
  onSelect,
}: {
  template: Template;
  onSelect: () => void;
}) {
  return (
    <div
      onClick={onSelect}
      className="group bg-slate-800/50 rounded-xl overflow-hidden border border-white/5 hover:border-orange-500/50 cursor-pointer transition-all hover:-translate-y-1"
    >
      {/* Preview image */}
      <div className="aspect-[16/10] bg-slate-700 relative overflow-hidden">
        <img
          src={template.thumbnail}
          alt={template.name}
          className="w-full h-full object-cover object-top"
        />
        <div className="absolute inset-0 bg-gradient-to-t from-slate-900/80 to-transparent opacity-0 group-hover:opacity-100 transition-opacity flex items-end justify-center pb-4">
          <span className="px-4 py-2 bg-orange-500 text-white font-medium rounded-lg">
            Use Template
          </span>
        </div>
      </div>

      {/* Info */}
      <div className="p-4">
        <h3 className="text-lg font-semibold text-white">{template.name}</h3>
        <p className="text-sm text-white/50 mt-1">{template.description}</p>
        <div className="flex gap-2 mt-3">
          {template.tags.slice(0, 3).map((tag) => (
            <span
              key={tag}
              className="px-2 py-0.5 bg-slate-700 text-xs text-white/60 rounded"
            >
              {tag}
            </span>
          ))}
        </div>
      </div>
    </div>
  );
}

function BlankTemplateCard({ onSelect }: { onSelect: () => void }) {
  return (
    <div
      onClick={onSelect}
      className="group bg-slate-800/30 rounded-xl overflow-hidden border border-dashed border-white/10 hover:border-orange-500/50 cursor-pointer transition-all flex flex-col items-center justify-center min-h-[300px]"
    >
      <div className="w-16 h-16 rounded-full bg-slate-700 flex items-center justify-center mb-4 group-hover:bg-orange-500/20 transition-colors">
        <svg
          className="w-8 h-8 text-white/40 group-hover:text-orange-400 transition-colors"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
        >
          <path
            strokeLinecap="round"
            strokeLinejoin="round"
            strokeWidth={2}
            d="M12 4v16m8-8H4"
          />
        </svg>
      </div>
      <h3 className="text-lg font-semibold text-white">Start from Scratch</h3>
      <p className="text-sm text-white/50 mt-1">Build your own custom design</p>
    </div>
  );
}
```

### 5. Template Preview Modal

**File**: `website/components/configurator/TemplatePreviewModal.tsx`

Show full preview before selecting:

```tsx
'use client';

import { Template } from '@/lib/patterns/templates';
import { DynamicPreviewRenderer } from '@/components/preview/DynamicPreviewRenderer';

interface TemplatePreviewModalProps {
  template: Template | null;
  isOpen: boolean;
  onClose: () => void;
  onConfirm: () => void;
}

export function TemplatePreviewModal({
  template,
  isOpen,
  onClose,
  onConfirm,
}: TemplatePreviewModalProps) {
  if (!isOpen || !template) return null;

  return (
    <div className="fixed inset-0 z-50 flex">
      {/* Backdrop */}
      <div className="absolute inset-0 bg-black/80" onClick={onClose} />

      {/* Modal */}
      <div className="relative flex flex-col w-full h-full m-4 bg-slate-900 rounded-2xl overflow-hidden">
        {/* Header */}
        <div className="flex items-center justify-between p-4 border-b border-white/10">
          <div>
            <h2 className="text-xl font-semibold text-white">{template.name}</h2>
            <p className="text-sm text-white/50">{template.description}</p>
          </div>
          <div className="flex gap-3">
            <button
              onClick={onClose}
              className="px-4 py-2 text-white/60 hover:text-white"
            >
              Cancel
            </button>
            <button
              onClick={onConfirm}
              className="px-6 py-2 bg-orange-500 hover:bg-orange-600 text-white font-medium rounded-lg"
            >
              Use This Template
            </button>
          </div>
        </div>

        {/* Preview */}
        <div className="flex-1 overflow-auto bg-slate-950">
          <DynamicPreviewRenderer
            definition={template.definition}
            viewport="desktop"
            editable={false}
          />
        </div>
      </div>
    </div>
  );
}
```

---

## Template Requirements

Each template should include:

1. **Complete branding**: Colors, fonts, spacing
2. **All section types**: Hero, features, testimonials, pricing, FAQ, CTA
3. **Placeholder content**: Realistic but generic text
4. **Meta tags**: Title and description for SEO
5. **Category tags**: For filtering

---

## Template Count Target

MVP: 5 templates (one per category)
- SaaS Pro (dark theme)
- Creative Agency (light theme)
- E-commerce Store
- Portfolio Minimal
- Startup Launch

Post-MVP: Add 2-3 more per category

---

## Success Criteria

- [ ] Template gallery shows 5+ templates
- [ ] Category filter works
- [ ] Preview modal shows full template
- [ ] "Start from Scratch" option available
- [ ] Selected template loads into configurator
- [ ] Branding and content editable after selection

---

## Testing

1. Navigate to configurator
2. See template gallery
3. Filter by "SaaS"
4. Click template to preview
5. Click "Use This Template"
6. Template loads with all sections
7. Edit headline - change persists

---

*Created by Platform Agent*

