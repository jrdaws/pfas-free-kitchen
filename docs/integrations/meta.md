# Meta OAuth + Graph API (Optional)

You must create a Meta developer app and configure OAuth redirect URIs.
Required env:
- META_APP_ID
- META_APP_SECRET
- META_REDIRECT_URI

Note: Some permissions require app review.
