ACTIVATION: w10-ui-polish

═══════════════════════════════════════════════════════════════════════════════
ROLE: Frontend Engineer Agent  
MISSION: Polish UI components, fix styling issues, and ensure visual consistency
═══════════════════════════════════════════════════════════════════════════════

CONTEXT (Refinement Wave)
The frontend exists but may have:
- Inconsistent styling
- Missing hover/focus states
- Broken responsive layouts
- Incomplete error/empty states

EXISTING CODE
- Frontend: src/pfas-web/

───────────────────────────────────────────────────────────────────────────────
DELIVERABLES
───────────────────────────────────────────────────────────────────────────────

1. DESIGN TOKENS / CSS VARIABLES
   File: src/pfas-web/app/globals.css

   ```css
   :root {
     /* Colors - Trust-focused palette */
     --color-primary: #1a5f4a;       /* Forest green - trust, nature */
     --color-primary-light: #2d8a6b;
     --color-primary-dark: #134438;
     
     --color-accent: #d4a574;        /* Warm copper - kitchen feel */
     --color-accent-light: #e8c9a8;
     
     /* Tier colors */
     --color-tier-0: #6b7280;        /* Gray - unknown */
     --color-tier-1: #cd7f32;        /* Bronze */
     --color-tier-2: #c0c0c0;        /* Silver */
     --color-tier-3: #ffd700;        /* Gold */
     --color-tier-4: #e5e4e2;        /* Platinum */
     
     /* Semantic colors */
     --color-success: #16a34a;
     --color-warning: #ca8a04;
     --color-error: #dc2626;
     --color-info: #2563eb;
     
     /* Neutrals */
     --color-bg: #fafaf9;
     --color-bg-alt: #f5f5f4;
     --color-surface: #ffffff;
     --color-border: #e7e5e4;
     --color-text: #1c1917;
     --color-text-muted: #78716c;
     
     /* Typography */
     --font-sans: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
     --font-display: 'Fraunces', Georgia, serif;
     
     /* Spacing */
     --space-xs: 0.25rem;
     --space-sm: 0.5rem;
     --space-md: 1rem;
     --space-lg: 1.5rem;
     --space-xl: 2rem;
     --space-2xl: 3rem;
     
     /* Radii */
     --radius-sm: 4px;
     --radius-md: 8px;
     --radius-lg: 12px;
     --radius-full: 9999px;
     
     /* Shadows */
     --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
     --shadow-md: 0 4px 6px rgba(0, 0, 0, 0.07);
     --shadow-lg: 0 10px 15px rgba(0, 0, 0, 0.1);
     
     /* Transitions */
     --transition-fast: 150ms ease;
     --transition-normal: 250ms ease;
   }
   ```

2. TIER BADGE POLISH
   File: src/pfas-web/components/product/TierBadge.module.css

   ```css
   .badge {
     display: inline-flex;
     align-items: center;
     gap: var(--space-xs);
     padding: var(--space-xs) var(--space-sm);
     border-radius: var(--radius-full);
     font-size: 0.75rem;
     font-weight: 600;
     text-transform: uppercase;
     letter-spacing: 0.025em;
     transition: transform var(--transition-fast), box-shadow var(--transition-fast);
   }

   .badge:hover {
     transform: translateY(-1px);
     box-shadow: var(--shadow-md);
   }

   /* Tier variants */
   .tier0 {
     background: linear-gradient(135deg, #e5e5e5 0%, #d4d4d4 100%);
     color: #525252;
     border: 1px solid #a3a3a3;
   }

   .tier1 {
     background: linear-gradient(135deg, #f4d19b 0%, #cd7f32 100%);
     color: #451a03;
     border: 1px solid #b8860b;
   }

   .tier2 {
     background: linear-gradient(135deg, #e8e8e8 0%, #c0c0c0 100%);
     color: #1f2937;
     border: 1px solid #9ca3af;
   }

   .tier3 {
     background: linear-gradient(135deg, #fef08a 0%, #fbbf24 100%);
     color: #713f12;
     border: 1px solid #d97706;
   }

   .tier4 {
     background: linear-gradient(135deg, #f0f0f0 0%, #e5e4e2 100%);
     color: #1f2937;
     border: 2px solid var(--color-primary);
     box-shadow: 0 0 0 2px rgba(26, 95, 74, 0.2);
   }

   /* Size variants */
   .small {
     padding: 2px 6px;
     font-size: 0.65rem;
   }

   .large {
     padding: var(--space-sm) var(--space-md);
     font-size: 0.875rem;
   }

   /* Icon */
   .icon {
     font-size: 1em;
   }
   ```

3. PRODUCT CARD POLISH
   File: src/pfas-web/components/product/ProductCard.module.css

   ```css
   .card {
     display: flex;
     flex-direction: column;
     background: var(--color-surface);
     border: 1px solid var(--color-border);
     border-radius: var(--radius-lg);
     overflow: hidden;
     transition: transform var(--transition-normal), box-shadow var(--transition-normal);
   }

   .card:hover {
     transform: translateY(-4px);
     box-shadow: var(--shadow-lg);
   }

   .card:focus-within {
     outline: 2px solid var(--color-primary);
     outline-offset: 2px;
   }

   .imageContainer {
     position: relative;
     aspect-ratio: 1;
     background: var(--color-bg-alt);
     overflow: hidden;
   }

   .image {
     object-fit: cover;
     transition: transform var(--transition-normal);
   }

   .card:hover .image {
     transform: scale(1.05);
   }

   .tierBadge {
     position: absolute;
     top: var(--space-sm);
     right: var(--space-sm);
   }

   .content {
     padding: var(--space-md);
     display: flex;
     flex-direction: column;
     gap: var(--space-xs);
     flex: 1;
   }

   .name {
     font-family: var(--font-display);
     font-size: 1rem;
     font-weight: 600;
     color: var(--color-text);
     line-height: 1.3;
     display: -webkit-box;
     -webkit-line-clamp: 2;
     -webkit-box-orient: vertical;
     overflow: hidden;
   }

   .name a {
     color: inherit;
     text-decoration: none;
   }

   .name a:hover {
     color: var(--color-primary);
   }

   .brand {
     font-size: 0.875rem;
     color: var(--color-text-muted);
   }

   .materials {
     font-size: 0.8rem;
     color: var(--color-text-muted);
     margin-top: auto;
   }

   .retailers {
     display: flex;
     gap: var(--space-xs);
     margin-top: var(--space-sm);
     flex-wrap: wrap;
   }

   .evidenceIndicator {
     display: inline-flex;
     align-items: center;
     gap: 4px;
     font-size: 0.75rem;
     color: var(--color-success);
     background: rgba(22, 163, 74, 0.1);
     padding: 2px 8px;
     border-radius: var(--radius-full);
   }

   .compareToggle {
     position: absolute;
     top: var(--space-sm);
     left: var(--space-sm);
     opacity: 0;
     transition: opacity var(--transition-fast);
   }

   .card:hover .compareToggle,
   .card:focus-within .compareToggle,
   .compareToggle[aria-pressed="true"] {
     opacity: 1;
   }
   ```

4. LOADING SKELETON
   File: src/pfas-web/components/ui/Skeleton.tsx

   ```tsx
   import styles from './Skeleton.module.css';
   import clsx from 'clsx';

   interface SkeletonProps {
     variant?: 'text' | 'circular' | 'rectangular';
     width?: string | number;
     height?: string | number;
     className?: string;
   }

   export function Skeleton({ variant = 'rectangular', width, height, className }: SkeletonProps) {
     return (
       <div 
         className={clsx(styles.skeleton, styles[variant], className)}
         style={{ width, height }}
         aria-hidden="true"
       />
     );
   }

   export function ProductCardSkeleton() {
     return (
       <div className={styles.cardSkeleton}>
         <Skeleton variant="rectangular" height={200} />
         <div className={styles.content}>
           <Skeleton width="80%" height={20} />
           <Skeleton width="50%" height={16} />
           <Skeleton width="60%" height={14} />
         </div>
       </div>
     );
   }

   export function ProductGridSkeleton({ count = 12 }: { count?: number }) {
     return (
       <div className={styles.gridSkeleton}>
         {Array.from({ length: count }).map((_, i) => (
           <ProductCardSkeleton key={i} />
         ))}
       </div>
     );
   }
   ```

   File: src/pfas-web/components/ui/Skeleton.module.css
   ```css
   .skeleton {
     background: linear-gradient(
       90deg,
       var(--color-bg-alt) 25%,
       var(--color-bg) 50%,
       var(--color-bg-alt) 75%
     );
     background-size: 200% 100%;
     animation: shimmer 1.5s infinite;
     border-radius: var(--radius-sm);
   }

   @keyframes shimmer {
     0% { background-position: 200% 0; }
     100% { background-position: -200% 0; }
   }

   .circular {
     border-radius: 50%;
   }

   .text {
     border-radius: var(--radius-sm);
     height: 1em;
   }

   .cardSkeleton {
     border: 1px solid var(--color-border);
     border-radius: var(--radius-lg);
     overflow: hidden;
   }

   .cardSkeleton .content {
     padding: var(--space-md);
     display: flex;
     flex-direction: column;
     gap: var(--space-sm);
   }

   .gridSkeleton {
     display: grid;
     grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
     gap: var(--space-lg);
   }
   ```

5. ERROR STATE COMPONENT
   File: src/pfas-web/components/ui/ErrorState.tsx

   ```tsx
   import styles from './ErrorState.module.css';
   import { Button } from './Button';

   interface ErrorStateProps {
     type: 'api-error' | 'not-found' | 'search-unavailable';
     onRetry?: () => void;
   }

   const ERROR_CONTENT = {
     'api-error': {
       icon: '⚠️',
       title: 'Something went wrong',
       message: 'Unable to load data. Please try again.',
       showRetry: true,
     },
     'not-found': {
       icon: '🔍',
       title: 'Not found',
       message: 'The page or product you\'re looking for doesn\'t exist.',
       showRetry: false,
     },
     'search-unavailable': {
       icon: '🔎',
       title: 'Search unavailable',
       message: 'Search is temporarily unavailable. Try browsing by category.',
       showRetry: true,
     },
   };

   export function ErrorState({ type, onRetry }: ErrorStateProps) {
     const content = ERROR_CONTENT[type];

     return (
       <div className={styles.container} role="alert">
         <span className={styles.icon}>{content.icon}</span>
         <h2 className={styles.title}>{content.title}</h2>
         <p className={styles.message}>{content.message}</p>
         {content.showRetry && onRetry && (
           <Button onClick={onRetry} variant="primary">
             Try Again
           </Button>
         )}
       </div>
     );
   }
   ```

6. EMPTY STATE COMPONENT
   File: src/pfas-web/components/ui/EmptyState.tsx

   ```tsx
   import styles from './EmptyState.module.css';
   import Link from 'next/link';

   interface EmptyStateProps {
     type: 'no-results' | 'no-products' | 'empty-compare';
     filters?: Record<string, string>;
     onClearFilters?: () => void;
   }

   export function EmptyState({ type, filters, onClearFilters }: EmptyStateProps) {
     if (type === 'no-results') {
       return (
         <div className={styles.container}>
           <span className={styles.icon}>📭</span>
           <h2 className={styles.title}>No products match your filters</h2>
           <p className={styles.message}>Try adjusting your filters:</p>
           {filters && Object.keys(filters).length > 0 && (
             <ul className={styles.filterList}>
               {Object.entries(filters).map(([key, value]) => (
                 <li key={key}>
                   <span>{key}: {value}</span>
                 </li>
               ))}
             </ul>
           )}
           {onClearFilters && (
             <button onClick={onClearFilters} className={styles.clearButton}>
               Clear all filters
             </button>
           )}
         </div>
       );
     }

     if (type === 'empty-compare') {
       return (
         <div className={styles.container}>
           <span className={styles.icon}>⚖️</span>
           <h2 className={styles.title}>No products to compare</h2>
           <p className={styles.message}>
             Add products to compare by clicking the compare checkbox on product cards.
           </p>
           <Link href="/cookware" className={styles.link}>
             Browse products →
           </Link>
         </div>
       );
     }

     return null;
   }
   ```

7. RESPONSIVE GRID
   File: src/pfas-web/components/product/ProductGrid.module.css

   ```css
   .grid {
     display: grid;
     grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
     gap: var(--space-lg);
   }

   @media (max-width: 640px) {
     .grid {
       grid-template-columns: repeat(2, 1fr);
       gap: var(--space-md);
     }
   }

   @media (max-width: 400px) {
     .grid {
       grid-template-columns: 1fr;
     }
   }

   .header {
     display: flex;
     justify-content: space-between;
     align-items: center;
     margin-bottom: var(--space-lg);
     flex-wrap: wrap;
     gap: var(--space-md);
   }

   .count {
     font-size: 0.875rem;
     color: var(--color-text-muted);
   }

   .sortSelect {
     font-size: 0.875rem;
     padding: var(--space-xs) var(--space-sm);
     border: 1px solid var(--color-border);
     border-radius: var(--radius-md);
     background: var(--color-surface);
   }
   ```

───────────────────────────────────────────────────────────────────────────────
CHECKLIST
───────────────────────────────────────────────────────────────────────────────

□ All tier badges have correct colors
□ Product cards have hover states
□ Focus states visible on interactive elements
□ Loading skeletons display during data fetch
□ Error states show when API fails
□ Empty state shows when no results
□ Grid is responsive at all breakpoints
□ Typography is consistent
□ Colors match design tokens

───────────────────────────────────────────────────────────────────────────────
TEST AT BREAKPOINTS
───────────────────────────────────────────────────────────────────────────────

- 1440px (Desktop large)
- 1024px (Desktop)
- 768px (Tablet)
- 640px (Mobile landscape)
- 375px (Mobile portrait)
